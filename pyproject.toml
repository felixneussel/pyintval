[project]
name = "pyintval"
version = "0.1.0"
description = "Seamless interval arithmetic"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []


[tool.setuptools]
py-modules = ['interval_arithmetic']
